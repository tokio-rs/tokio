[build]
  command = """
    rustup install nightly --profile minimal && cargo doc --no-deps
    """
  publish = "target/doc"

[build.environment]
  RUSTDOCFLAGS="""
    -D warnings \
    --cfg docsrs \
    """
